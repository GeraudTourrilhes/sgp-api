package dev.controller;

public class CollaborateurController {

	/*
	 * GET /api/collaborateurs : retourne la liste des collaborateurs au format
	 * JSON. GET /api/collaborateurs?departement=[ID_DEPARTEMENT]: retourne la
	 * liste des collaborateurs au format JSON dont le département a
	 * l'identifiant ID_DEPARTEMENT. GET /api/collaborateurs/[MATRICULE] :
	 * retourne les informations d'un collaborateur. PUT
	 * /api/collaborateurs/[MATRICULE] : modifie un collaborateur (données
	 * envoyées au format JSON). GET /api/collaborateurs/[MATRICULE]/banque :
	 * récupère les coordonnées bancaires d'un collaborateur (nom de la banque,
	 * iban et bic) PUT /api/collaborateurs/[MATRICULE]/banque : mo
	 */
}
